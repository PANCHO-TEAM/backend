version: '3.8'
name: cats-on-cakes

services:
  # Сервис PostgreSQL хранит данные приложения.
  postgres:
    image: postgres:16-alpine
    container_name: cats-on-cakes-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cats_on_cakes
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Backend-сервис собирается из текущего проекта и подключается к базе по имени сервиса.
  backend:
    build: .
    container_name: cats-on-cakes-backend
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/cats_on_cakes?schema=public
    depends_on:
      - postgres

# Именованный том сохраняет данные PostgreSQL между перезапусками.
volumes:
  postgres_data:
